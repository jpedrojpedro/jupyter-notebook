services:
  jupyter:
    image: jupyter/scipy-notebook:x86_64-ubuntu-22.04
    user: root
    shm_size: '4gb'
    environment:
      - CHOWN_HOME=yes
      - JUPYTER_KSC_MAX_WORKERS=4
    mem_swappiness: 0
    volumes:
      - ./:/home/jovyan/work/
    ports:
      - 8888:8888
    deploy:
      resources:
        limits:
          memory: 8GB
    command: >
      bash -c "pip install --upgrade nbformat 'numpy<2' pandas numexpr bottleneck tabulate duckdb &&
      start-notebook.sh --NotebookApp.token=''
      --NotebookApp.iopub_data_rate_limit=1.0e10
      --ServerApp.shutdown_no_activity_timeout=0
      --ServerApp.answer_yes=True"
